/// MEJORA LA SALUD DEL PLAYER.

// player_heal(+health);

if (!alive) {exit}

hlth += argument0;  // Recuperarse.
infected = 0;       // Mejorarse.
if (argument0 > 0)  // Alegrarse.
   {
   //Player_Talk(P_tlk,S_get);
   }
